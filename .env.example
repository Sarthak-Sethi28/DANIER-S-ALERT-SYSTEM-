# Danier Alert System - Environment Variables
# Copy to .env and fill in values for local development

# ============ FRONTEND ============
# Backend API URL (use production for testing against live API)
REACT_APP_API_BASE_URL=https://danier-s-alert-system.onrender.com

# For local dev with local backend, use:
# REACT_APP_API_BASE_URL=http://localhost:8000

# ============ BACKEND ============
# Database (SQLite for local)
DATABASE_URL=sqlite:///./danier_stock_alert.db

# Email (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
DEFAULT_RECIPIENT_EMAIL=alerts@danier.ca

# CORS - Frontend URL
FRONTEND_URL=http://localhost:3000

# Upload directory
UPLOAD_DIR=./uploads
